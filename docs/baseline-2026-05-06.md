# Baseline — 2026-05-06 (Day 0, read-only)

Captured before any changes from `memoized-dancing-stonebraker.md` plan.

## Git state
- Branch: `feat/novedades-authoring-pipeline` (HEAD detached, 12 commits ahead of main)
- Uncommitted: README.md (M), REPORTE_EJECUCION_GATE1.md (A), WORKSHOP_EJECUTIVO_2026-05-04.md (A), supabase/.temp/cli-latest (A)

## Routers — line counts
| File | Lines | Budget? |
|---|---|---|
| families.ts | 1632 | 4× over |
| announcements.ts | 1247 | 3× over |
| persons.ts | 592 | 1.5× over |
| programs.ts | 359 | OK |
| dashboard.ts | 341 | OK |
| checkin.ts | 282 | OK |
| entregas.ts | 265 | OK |
| admin.ts | 247 | OK |
| families-csv-members-procedures.ts | 216 | OK |
| ocr.ts | 179 | OK |
| persons.documento-extranjero.test.ts | 133 | OK (test) |
| families-members.ts | 100 | OK |

A.0a target: split `families.ts` and `announcements.ts`. `persons.ts` is over budget but not on the critical path; flagged for follow-up.

## Migrations — count
- Local branch: 13 migrations under `supabase/migrations/`
- GitHub `main`: ~30+ migrations under `supabase/migrations/EXPORTED/` (the JSONB→table migration for `family_members`, `consent_templates`, `program_sessions`, `consents`, `deliveries`, etc. all already applied on `main`)

## Tests
- 59 test files, 765 tests total
- **745 passing, 13 failing, 7 skipped**
- All 13 failures trace to missing env vars: `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`
- Failing test files (4): `supabase-env.test.ts`, others touched by the same admin-client bootstrap path

## Typecheck (`pnpm check`)
**Failing.** Errors found in:
- `client/src/components/DeliveryDocumentUpload.tsx` — multiple `TS2339: Property 'data' does not exist`, `TS2322: Type ... not assignable`, `TS2739: missing properties`
- `server/routers/families.ts:1094` — `TS2322: Type 'string | null' not assignable to type 'string'`

## Lint (`pnpm lint`)
**Failing.** 167 problems — 76 errors + 91 warnings.
- `families.ts`: ~9 `no-explicit-any` errors
- `programs.ts`: 3 `no-explicit-any` errors
- `persons.ts`: 2 `no-explicit-any` errors + unused vars
- `ocr.ts`: warnings (unused TRPCError, console statements)
- Many other minor warnings (unused vars, console statements)

Per CLAUDE.md §3: "TypeScript strict mode · No `any` · No `as unknown as X`". Active drift.

## Implications for the plan
1. **Day 0 baseline reveals the build is not green on this branch.** Running `pnpm check` and `pnpm lint` both exit 1.
2. **Phase A.0a (split files) cannot run cleanly** until at least typecheck is green — splitting broken code propagates the breakage across new modules and makes diffs unreviewable.
3. **Test failures are environment-only** (missing env vars). Solvable via `.env.test` or CI secrets; no code change required.
4. **The 76 lint errors are pre-existing.** Per Karpathy's surgical-changes rule, these are NOT in scope for this plan unless they touch a file we're already splitting. Most are in `families.ts` (will be addressed during A.0a split) and `announcements.ts` (same).

## Recommended next step (request Leo's call)
Before Day 1 (CLAUDE.md update) or A.0a (file split), need a decision on:

- **Option A (recommended): Fix typecheck first.** The 9 type errors in `DeliveryDocumentUpload.tsx` and the `families.ts:1094` null check are blockers. Quick surgical fixes (Karpathy: each error → minimal patch) before any further structural work. Estimated: ~1 hour.
- **Option B: Proceed despite red build.** Risk: A.0a split lands on top of broken types; Phase A test retrofits would require fixing them anyway. Not recommended.
- **Option C: Diagnose env-only test failures separately** by adding `.env.test` and re-running. If green-modulo-types, then Option A is even more tightly scoped.
